export interface LegistsState {
  legists: Legist[];
}

export interface Legist {
  id: string;
  name: string;
  firstName: string;
  lastName: string;
  // userPic: null;
  email: string;
  // gender: string;
  // locale: string;
  // status: string;
  // lastVisit: string;
  // roles: string[];
}